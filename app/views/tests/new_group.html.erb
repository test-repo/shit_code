<h1>New group</h1>

<%= form_for @test do |f| %>

  <div class="field">
    <%= f.label :add_group %><br>
    <%= f.text_field :group %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', root_path %>
